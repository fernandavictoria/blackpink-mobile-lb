import {StyleSheet} from "react-native"

export const colors = {
    preto: "#000",
    branco: "#ffff",
    cinza: "#808080",

}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})